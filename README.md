To remove previous neovim configuration:
```shell
rm -rf ~/.local/share/nvim ~/.local/share/nvim.bak
rm -rf ~/.local/state/nvim ~/.local/state/nvim.bak
rm -rf ~/.cache/nvim ~/.cache/nvim.bak
rm -rf ~/.local/share/nvim/tree-sitter-*
``
